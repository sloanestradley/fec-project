# Testing — FECLedger

Playwright-based automated tests. Two tracks: structural (mocked API, fast, runs every session) and smoke (live API, slow, run manually before deploys).

---

## Quick start

```bash
# Install dependencies (one-time)
npm install
npx playwright install chromium

# Run Track 1 — structural tests (default, mocked API)
npx playwright test

# Run Track 2 — smoke tests (live FEC API, run manually)
npm run test:smoke
# or: npx playwright test --config playwright.smoke.config.js

# Open the last HTML report
npm run test:report
```

---

## Track 1 — Structural tests (mocked API)

**Command:** `npx playwright test` or `npm test`
**When to run:** Every session, before and after making changes.
**Speed:** ~2.9 minutes for all 878 tests.

### What they test

All tests mock the FEC API (instant responses, no network) and the Amplitude SDK (events captured in-memory via the snippet's `_q` queue).

| File | What's covered |
|------|----------------|
| `tests/shared.spec.js` | Shared checks for every page (10 pages): `styles.css` linked, `main.js` linked, top nav present with two nav links (Races/Feed) scoped to `.top-nav-links`, mobile search toggle present, warm parchment background, `Page Viewed` Amplitude event fires, nav logo `.logo-fec`/`.logo-ledger` spans present, `.global-banner` precedes `.top-nav` in DOM, desktop nav has a search button (`#nav-search-btn`, type=button — T-search-overlay replaced the nav search input), `#mobile-nav` is a child of `.top-nav`, mobile nav has two links |
| `tests/candidate.spec.js` | Profile header (including `#race-context` DOM presence, `.race-context-line` flex structure with `.race-context-line-label`, `.race-context-line-text`, and link (label content matches `formatRaceLabelLong()`), `#profile-header-sentinel` present for compact scroll observer, T14.5 Cycle card chevron renders on cycle detail / hidden on cycle index, Cycle card chevron aria-label = "Back to all cycles", Cycle card chevron click on fresh-load detail returns to cycle index, `#stat-cycle` shows single election year (T-cycle-semantics, 2026-05-19, was "year-range"), race tag (`.tag-neutral` via `formatRaceName()`) present in `#meta-row` between FEC ID and party, no `.candidate-race-label`, meta-row canonical order FEC ID → race → party → incumbent, scroll down adds `.compact` class, scroll to top removes `.compact` class, `.main` paddingBottom set when compact and cleared on un-compact), stats row (non-$0 financials), health banner, `#summary-strip` persistence on the flowing detail view (no outer tabs — T-remove-profile-tabs), flow order summary→raised→spent→page-note + exactly-one-Chart-instance-per-canvas lock, first stat card label = "Election" (T-cycle-semantics, 2026-05-19, was "Cycle"), chart canvas, chart legend (3 money-series items only — filing deadline + election day overlays removed 2026-04-24), network assertion (neither `/reporting-dates/` nor `/election-dates/` called), flowing detail view (no outer tabs; nested Raised sub-tabs + committees modal still switch), committees modal, Amplitude events (incl. `Tab Switched` no longer fires), legacy `#cycle#tab` back-compat → bare `#cycle`, API correctness (no 422 errors), Raised tab contributors: `#donors-tbody` non-empty + `.raised-cell-title` reads "Raised breakdown", Top Conduit Sources card (`#conduits-card` visible + `#conduits-tbody` contains the mocked ActBlue memo row); **index view landing state**: bare URL → `#career-strip` + `#cycle-index` visible, `#content` + summary-strip hidden; `#cycles` hash → also index view; CareerStrip three labels (T14): History / Career Raised / Career Spent; History cell shows year or year-range; cycle index row count matches fixture; `Page Viewed` fires with `view: 'index'`; cycle row href format; single-year label format (T-cycle-semantics, 2026-05-19); `#profile-menu-btn` visible (was `#committees-trigger` pre-T-menu-btn-profile-header, 2026-05-26); **detail view regression**: hash URL renders `#content` + summary-strip, career-strip hidden; **archive threshold**: pre-2008 rows are non-navigable divs with `tabindex=-1`, archive divider present; **in-place transitions (T6.5)**: no page reload on cycle-row click (DOM identity check via dataset.mark), `#profile-header` DOM node preserved across transition, index elements hidden after transition, back button returns to index, back button does not re-fetch cached data, index→detail compact-engaged case scrolls to compactThreshold (uses `cycle-index` padding inflation pattern as of 2026-04-28 — body.minHeight scaffolding masked the scrollTo-clamp class of bug; see committee.spec.js for the second-instance fix), index→detail non-compact case scrolls to 0, compact header active on index-only visit (no prior detail visit), back→index re-engages compact when restored scroll is past threshold, fetch race condition (last-clicked cycle wins); **path-segment URL ID extraction** (T10 parity, 2026-04-28): no-slash URL extracts ID, trailing-slash URL extracts ID, no-ID clean URL `/candidate` shows friendly "Browse candidates →" link, no-ID `.html` URL shows same friendly link, non-existent cycle year (`#1999#summary`) falls through to index view, legacy trailing segment (`#2024#bogus`) is ignored → canonicalizes to bare `#2024`; **T11 committees-modal lazy fetch** (2026-04-29): trigger reads `Committees →` (no count); zero `/committees/` calls fire on cycle load; first modal open fires both calls; second modal open hits cache (no refire); page reload re-fires on first open; `Committees Modal Opened` Amplitude still fires on click; **T12 progressive loading** (2026-05-05): donut renders synchronously; slow-tier skeletons visible during in-flight; no skeleton flash when fetch already resolved; per-tier failure renders error UI with retry; skeleton heights ≥200px (scroll-clamp guard); spent retry refires + content resolves; **T12.5 429-aware error UI**: init-stage 429 on `/committees/?cycle=` shows rate-limit copy on Raised + Spent tabs; init-stage 500 shows init-failure copy; tab-fetch 429 hides retry button; tab-fetch non-429 keeps retry button (regression); cycle switch after 429 clears error state; donut skeleton present in DOM and hides after donut renders; **T-load-3 stats-grid skeletons** (2026-05-19): cycle-detail stat cells (raised/spent/coh/ratio) have skeleton spans in initial HTML (regex match on raw HTML), `#stat-cycle` has NO skeleton in initial HTML and hydrates synchronously from URL hash before any await (T-cycle-semantics, 2026-05-19), replaced by real values after loadCycle resolves; cycle-index career cells (cstat-history/raised/spent) have skeleton spans in initial HTML, replaced by real values after fetchIndexData resolves; **T-load-4a progressive cycle-index** (2026-05-19): scaffold renders cycle-row year labels + skeleton financial cells during /totals/ fetch (real labels via T-cycle-semantics, skeleton cells until /totals/ resolves), hydration replaces skeletons with real values, back navigation reuses cached promise (no refire), partial-data /totals/ failure renders .tab-error retry UI with dashed cells, retry click refires and hydrates on success; **T-history-retire regression lock** (2026-05-19): /candidate/{id}/history/ is NOT called on cycle-index landing or cycle-detail landing — locks the architectural retirement so a future regression that reintroduces the call gets caught; **T-loadcycle-single-fetch regression lock** (2026-05-19): cycle-detail fires exactly one /totals/?cycle= call (was 1-3 parallel iterate-and-sum calls; now single election_full=true call returns FEC's pre-aggregated record); **T-load-4b chart-card skeleton** (2026-05-20): initial-HTML overlay presence (#chart-area at 320px height floor + #chart-skeleton + #chart-error overlays), post-renderChart hydration clears skeleton + height floor, loadCycle catch path resolves skeleton to "Unable to load chart" inline-status-msg via #chart-error overlay; **T-load-header-title-skeleton regression lock** (2026-05-20): name skeleton renders at non-zero width during entity-fetch await window (was a latent bug — width:60% inside .page-title flex item resolved to 0 via CSS circular-percentage-ref; fixed to width:8em — proportional to title font-size); **T-menu-btn-profile-header** (2026-05-26): profile menu-btn renders + opens with 5 items (Profile / Race(YYYY) / Committees / Compare / Follow); Race item label + href update on cycle change via `updateMenuState` re-fire from `trackPageViewed`; Profile item disabled on detail view (no-op) and enabled on index view; menu-btn visible in compact header (no hide rule); page-specific text label "Candidate" (preserved across `setShowText` resize toggling); race-context-bar "View race →" link repointed through `raceHref()` (Presidential `state=US`, at-large House district=00); committees-trigger replaced by menu-btn's Committees item (selector migration via `openCommittees()` helper across 17 prior call sites); **T-menu-btn-focus-on-open** (2026-05-26): open() focuses the dropdown container (not first item) — regression-lock for the Safari mobile :focus-visible touch-tap highlight bug; first ArrowDown enters at item 0, first ArrowUp at last item, no wrap; **T-modal-a11y** (2026-05-26): committees modal focus trap (Tab + Shift+Tab wraps within modal once `.modal-tabs` are populated), body scroll-lock, background `inert` on every non-modal `<body>` child, Escape closes (scoped to modal), outside-click on overlay closes, focus returns to menu-btn trigger on close, `role="dialog"` + `aria-modal="true"` + `aria-labelledby`, modal X has `aria-label="Close"`; **T-menu-btn-profile-header-title-wrap-fix** (2026-05-26): long candidate name wraps internally inside `.profile-header-row` and the menu-btn stays pinned top-right (does not break onto its own line) — regression-lock for the pre-existing flex-wrap bug; **Money flow (Sankey) mount** (Step 2, 2026-06-09): `#money-flow-card` renders an ECharts SVG on a data-present cycle + skeleton/gate clear, cohStart-derivation info-tooltip mounts on the title, no `#sankey-debt` caption on a zero-debt cycle, `#sankey-debt` "Debt at close: $X" renders on a positive-debt route override; **Money flow gate (presidential)**: office-`P` metadata override renders the "not yet modeled" gate, not the chart; **donut transfers wedge**: includes `transfers_from_affiliated_committee` (Form-3P) in the presidential-fallback donut |
| `tests/search.spec.js` | Initial state (search input present, plain-search-input ARIA — no combobox role/`aria-controls`, `aria-live` status region present, floating `#typeahead-dropdown` retired, `.search-field` + sr-only submit button); **live inline results** (T-search-inline-results — debounced query renders into `#state-results` in the page body, not a floating dropdown): a 2-char query fires no fetch and shows no `#state-error` (FEC API 3-char keyword minimum — regression-lock), 3+ chars renders `.results-group[data-group="candidates"]` + `[data-group="committees"]`, candidate/committee result links resolve, Enter after live results does not blank the results (`query()` dedup); **submit path** (result links, `Candidate Searched` Amplitude on Enter); `?q=` auto-search; **View all links** (`.results-view-all` → `/candidates?q=` / `/committees?q=` when count > 5); no-results state; fetch-error state (`#state-error` + retry button) |
| `tests/committee.spec.js` | **Detail view** (cycle-anchored URL `#2024#summary`): profile header, FEC ID tag, stats grid (#summary-strip), no outer tabs (de-tabbed — T-remove-profile-tabs), all three sections in-flow at once, `#summary-strip` persistence on the flowing detail view, exactly-one-Chart-instance-per-canvas lock, first stat card label = "Cycle" (T14), cycle changes via Cycle card chevron → cycle index → row click (T16 retired the in-tabs-bar `#cycle-switcher`), committee-name title-cased, .candidate-card-office absent, filing history stub absent, URL hash is bare `#cycle` (no tab segment — T-remove-profile-tabs), profile-header-sentinel present, T14.5 Cycle card chevron renders on cycle detail / hidden on cycle index, Cycle card chevron aria-label = "Back to all cycles", Cycle card chevron click on fresh-load detail returns to cycle index, `#stat-cycle` shows year-range, committee-header starts un-compact, index-view elements hidden in detail view; Raised tab: donut canvas + tooltip on candidate-contributions-and-loans segment, map container, individual donors tbody non-empty, committee donors card visible on a specific cycle, conduits card visible and populated, Top Individual Contributors card header includes cycle label (no "Most recent cycle" copy); Spent tab: donut canvas, spend-detail-bars, vendors tbody non-empty, contributions-section visible, contributions-tbody non-empty, vendors header shows cycle range (no "All time" label); **Index view landing state** (bare URL): `#career-strip` + `#cycle-index` visible, detail-view elements hidden; `#cycles` hash → also index view (NaN routing); CareerStrip 3 labels (T14): History / Lifetime Raised / Lifetime Spent; History cell shows year-range "2020–2026" from `first_file_date` + `last_file_date`; Lifetime Raised matches summed receipts ($7.6M from mock); Lifetime Spent matches summed disbursements; cycle-index row count matches `c.cycles`; rows sorted descending; cycle row labels use coverage_start_date for year range; cycle row click navigates to `#{year}#summary`; `Page Viewed` fires with `view: 'index'`; **Archive threshold** (per-test mock with pre-2008 cycle): pre-2008 row renders as non-navigable `div.cycle-row--archive` with `tabindex="-1"`; archive divider precedes archive rows with "Archived cycles (totals only)" inline label (post-K17.a 2026-05-28; prior "FEC coverage begins 2008" explanation retired pending TOOLTIP-VIZ); post-2008 rows are navigable `a.cycle-row`; **All-time removal regressions**: cycle switcher has zero `value="all"` options and zero "All time" text; old `#all#summary` bookmarks land on index view (NaN fallthrough); no "All cycles" copy on detail view; no "All-cycle aggregate" copy in data note; **Terminated committee branch**: FEC ID tag still renders (the prior "Active since" prose / `.meta-prose` regression guard was retired in T21 follow-up when the prose was removed entirely); **Raised tab unavailable-state copy**: "Data not available due to high transaction volume." when Schedule A pages > threshold; **Associated-candidate section**: assoc-list link uses bare `/candidate/{id}` URL; **In-place transitions (T10)**: no page reload on cycle-row click (DOM identity check via dataset.loadId), `#committee-header` DOM node preserved across transition, back button returns to index, back button does not re-fetch metadata or all-totals, index→detail compact-engaged case scrolls past 0 (uses `cycle-index` padding inflation pattern — body.minHeight scaffolding masked the scrollTo-clamp class of bug; second instance after T6.5; fix: minHeight stays as floor, never cleared), index→detail non-compact case scrolls to 0, compact header active on index-only visit (no prior detail visit), rapid cycle hash navigation: last cycle wins in summary stats; **path-segment URL ID extraction** (T10 parity, 2026-04-28): no-slash URL extracts ID, trailing-slash URL extracts ID (was returning "No committee specified" before .filter(Boolean) fix), no-ID clean URL `/committee` shows friendly "Browse committees →" link (was extracting `'committee'` as a literal ID before page-name guard added), no-ID `.html` URL shows same friendly link, non-existent cycle year (`#1999#summary`) falls through to index view, legacy trailing segment (`#2024#bogus`) is ignored → canonicalizes to bare `#2024`; **T12 progressive loading** (2026-05-05): donut renders synchronously; slow-tier skeletons visible during in-flight; no skeleton flash when fetch already resolved; per-tier failure renders error UI with retry; skeleton heights ≥200px (scroll-clamp guard); spent retry refires + content resolves; **T12.5 429-aware error UI**: init-stage 429 on `/committee/{id}/totals/` falls through to page-level state-msg.error (no tab-error bridging needed on committee.html — tabs aren't revealed if init fails); init-stage 500 same; tab-fetch 429 on `/schedules/schedule_a/` hides retry button; tab-fetch non-429 keeps retry button (regression); cycle switch after 429 clears error state; donut skeleton attached to DOM and hides after donut renders; **T-load-3 stats-grid skeletons** (2026-05-19): cycle-detail raised/spent/coh cells have skeleton spans in initial HTML (regex match on raw HTML), `#stat-cycle` has NO skeleton in initial HTML and hydrates synchronously from URL hash before any await, cycle-detail values replace skeletons after totals resolve; cycle-index career cells (cstat-history/raised/spent) have skeleton spans in initial HTML, replaced by real values after index resolves; **T-load-4a progressive cycle-index** (2026-05-19): cstat-history entity-hydrated at scaffold (no skeleton on that cell — committee data is entity-only), career raised/spent cells hydrate after /totals/, empty-cycle case (c.cycles=[]) renders "No filing cycles on record." inside #cycle-index; **T-committee-init-defer-totals** (2026-05-20): cycle-index scaffold renders independent of /totals/ (init awaits only entity on index path; ~1500ms delayed totals does not block scaffold visibility); cycle-row click during /totals/ load shows stat skeletons until totals resolves, then renderStats re-fires via totalsP.then() populator; detail-URL cold load still awaits totals before view.switchTo (no dashed stats from empty ALL_TOTALS); **T-load-header-title-skeleton regression lock** (2026-05-20): name skeleton renders at non-zero width during entity-fetch await window (was a latent bug — width:60% inside .page-title flex item resolved to 0 via CSS circular-percentage-ref; fixed to width:8em — proportional to title font-size); **T-menu-btn-profile-header** (2026-05-26): committee menu-btn renders + opens with 3 items (Profile / Compare / Follow); Profile item disabled on detail view, enabled on index view; menu-btn visible in compact header; page-specific text label "Committee" (preserved across `setShowText` resize toggling); compact min-height parity (56px floor across `#profile-header.compact / #committee-header.compact / #race-header.compact`); **T-modal-a11y** (2026-05-26): committee.html does not directly use the modal helper (no committees modal on this page), but compact-header / menu-btn integration tests verify the trigger return-focus pattern works across both pages; **Money flow (Sankey) mount** (Step 3, 2026-06-09): `#money-flow-card` renders an ECharts SVG on the default Form-3 PCC committee + skeleton/gate clear, cohStart-derivation info-tooltip mounts on the title, no `#sankey-debt` caption on a zero-debt cycle, `#sankey-debt` "Debt at close: $X" renders on a positive-debt override, **no-activity guard**: a zero-receipts/zero-disbursements cycle leaves the section blank (chart + gate + debt all hidden, no empty chart); **Money flow gate (dual-account, Gate 1)**: `receipts !== fed_receipts` override renders the non-federal "not yet modeled" gate; **Money flow gate (presidential, Gate 2)**: committee-type-`P` metadata override renders the presidential gate; **donut transfers wedge**: Form-3X "Transfers in" relabel + `transfers_from_affiliated_committee` coalesce; **spent donut loan-repayments coalesce (Form-3 PCC)**: a `loan_repayments`-named (not `loan_repayments_made`) totals override surfaces in the "Loan Repayments" wedge with `fmt` value, proving it isn't dropped into the remainder "Other Disbursements" |
| `tests/pages.spec.js` | races.html (page header, filter bar fields, state combo ARIA semantics + native fallback, office combo ARIA + listbox + native, cycle combo ARIA + listbox + native, results area containers), race.html (candidate cards, financial figures, cycle-anchored links emit bare `#{year}` (no `#summary` — T-remove-profile-tabs); **de-tabbed (T-remove-profile-tabs, race final pass)**: no `#tabs-bar`/`.tab`, candidate list flows in `#race-content`, Insights deferred to Phase 4, race URL carries no hash; **T-race-inplace-cycle in-place switching**: no-reload sentinel-survives, switch Page Viewed, aria-live announce, empty-cycle inline state, scroll+compact preserved on same/longer, shorter-cycle scroll-clamp + compact-drop, non-destructive error rollback + 429 no-retry, popstate revert, bare-URL + snap `?year=` canonicalization; **T-race-page-UI candidate-card refinements**: `#race-candidates-label` uses `.raised-cell-title`, stats are a 3-col side-by-side grid on desktop, and the `#race-list` scoping does not leak to shared candidates-browse cards), candidates.html (auto-load, search input, clean URLs, state combo ARIA, office/party/cycle combos ARIA + listbox structure + native fallback, dynamic cycle rows in listbox, filter chips, URL sync, error state, ?q= search mode, enter-to-search: 3+ char Enter runs a search / sub-3-char keyword guard, #load-more-spinner and #end-of-results DOM presence), committees.html (state combo ARIA, type combo ARIA + listbox row count, auto-load, clean URLs, filter chips, URL sync, error state, enter-to-search: 3+ char Enter runs a search / sub-3-char keyword guard), feed.html (results render on load, results header count, column headers, feed row structure with committee link + report tag + FEC external link, office/window/report-type filter controls, filter chips, end-of-results, error/empty states hidden, office filter interaction narrows rows, report type filter interaction narrows rows), process-log.html, design-system.html (token tables, color swatches, component card attributes), index.html redirect, mobile layout at 390px (top-nav visible, search toggle visible) and 1280px (search toggle hidden), mobile nav toggle behavior at 390px (hamburger opens/closes #mobile-nav, mobile search toggle opens the search overlay); **T-menu-btn demo coverage** (2026-05-26): design-system.html menu-btn demo triggers open dropdowns (3 demo cards: showText·click / showText·keyboard / iconOnly·click); focus-on-container regression-lock (open() focuses the dropdown wrapper, not first item); arrow-nav clamps at ends (no wrap); Home/End jump; Escape closes + returns focus to trigger; outside-click closes; `raceHref()` four-case URL builder tests (Presidential `state=US`, at-large House `district=00`, Senate no-district, House normal); `iconSvg()` 5-glyph rendering + unknown-name fallback; info modal a11y mechanics (focus trap, body scroll-lock, background inert, Escape + outside-click close, return-focus to trigger via openAccessibleModal helper) |
| `tests/overlay.spec.js` | Global search overlay (T-search-overlay). **Structure:** overlay injected on every nav page, hidden by default, `role=dialog` + `aria-modal`, contains the `initSearchPanel` elements (`#overlay-search-input`/`-results`/`-loading`/`-no-results`/`-error`), labelled close button. **Open / close:** nav search button opens it; X / Escape / browser-back all close it; refresh-while-open lands closed. **Focus + a11y:** focus moves to the input on open, restored to the nav button on close, Tab wraps within the overlay (focus trap), background is `inert` while open and un-inert on close. **Events + history safety:** `Search Opened` Amplitude event fires on open; opening + closing on a profile page (`/candidate/…#2024#summary`) fires zero `hashchange` and preserves the URL (the state-only pushState regression-lock). |
| `tests/tooltip.spec.js` | Tooltip component (T-tooltip-component, 2026-05-29). Renders against `/design-system.html`. **Structure + aria:** trigger button has `aria-haspopup="true"`, `aria-expanded="false"`, `aria-label` transferred from host, outlined icon visible by default. **Interaction:** hover opens popup with `role="tooltip"` after ~100ms delay; `aria-describedby` matches popup id; icon flips to filled on reveal; click tap-toggle; Escape closes + returns focus; outside pointerdown closes; opening B closes A (singleton); scroll + resize close (with rAF-deferred listener attachment guarding against in-flight scroll); cursor handoff from trigger to popup keeps it open; Tab + Enter via keyboard opens with `:focus-visible` keeping icon outlined until reveal. **Positioning:** default zero-gap (popup top edge contiguous with trigger bottom edge); flips above when no room below; horizontal shift to fit viewport. **Reduced motion:** instant reveal. **Dev-time warnings:** missing aria-label → console.warn + skip wire; nested `<button>` ancestor → console.warn + proceed. 20 tests. |
| `tests/party-helpers.spec.js` | `partyClass` + `partyLabel` dual-field design (T-party-helpers-dual-field-rewrite, 2026-06-01). Renders against `/design-system.html`; page.evaluate calls helpers directly. **Mainstream parties** (DEM/REP/LIB/GRE/IND short codes + full names + both-set cross-surface invariant). **N/A bucket** — every short code (NNE/NON/UNK/OTH/NPA/UN/W/O) and every verified full name (NONE/NON-PARTY/UNKNOWN/OTHER/NO PARTY AFFILIATION/UNAFFILIATED/WRITE-IN). **Variant affiliates** — DFL (named after H8MN01279 Feehan) preserves full name + tag-dem hue via partyClass startsWith on party_full; deliberate asymmetry with partyLabel exact-match. **Cryptic short code without party_full** (PPP, H0NY03067 Ross case) → Party N/A + tag-neutral. **Cross-surface invariant block** — 6 real-candidate scenarios (Trump / Marie / Feehan / Macruari / Ross / Kanye) verify same-input-same-output regardless of which fields are populated. 38 tests. |
| `tests/candidate-card.spec.js` | `candidateCardHTML(c, opts)` consolidated builder (T-card-builder-consolidation, 2026-06-01). Renders against `/design-system.html`. **Default 'meta' layout** (candidates browse + search): 3-tag meta block (office + cycle + party); href `?from=`; onclick has 'Candidate Result Clicked' + candidate_name. **'inline' layout** (race.html shape): tags inline in name; stats row when totals non-null; cycleHashYear appends `#year#summary`; includeName:false omits candidate_name; extraTrackProps merges into payload; showStats with no totals defensively omits the row. **Committee assoc-card shape**: showLatestCycle:false omits cycle tag; trackEvent:null omits onclick entirely. **Robust extraction**: candidate_ids[0] fallback when candidate_id missing (race.html /elections/ shape); candidate_name fallback when name missing; href is `#` placeholder when both id fields missing. 13 tests. |
| `tests/race-helpers.spec.js` | `formatRaceName` + `formatRaceLabelLong` + `toOrdinal` (T-fixture-coverage-tier1-2, 2026-06-01). Renders against `/design-system.html`. **formatRaceName**: Presidential (state/district inputs ignored), Senate (district suppressed), at-large House (no district suffix), House with district, House teen district. **formatRaceLabelLong**: Presidential, Senate full-state-name, at-large House (district='00' AND empty), House with district, House teen district preserves ordinal ('11th'/'12th'/'13th'). **toOrdinal**: teen exception (11/12/13 → th; also 111/112/113), mod-10 rules (1=st, 2=nd, 3=rd, 0/4-9=th), string-input tolerance ('03' → '3rd'). 19 tests. `raceHref` coverage lives in `pages.spec.js` instead. |
| `tests/committee-helpers.spec.js` | `committeeTypeLabel` + `filingFrequencyLabel` + `filingFrequencyDotClass` (T-fixture-coverage-tier1-2, 2026-06-01). Renders against `/design-system.html`. **committeeTypeLabel**: every mapped code (P/J/D/O/Q/N/V/H/S/Y/I/U) → expected label; unmapped → 'Type X' fallback; case-sensitivity lock. **filingFrequencyLabel**: every mapped code (A/D/M/Q/T/W) → expected label; unmapped → raw fallback; null/empty/undefined → em-dash. **filingFrequencyDotClass**: T + A → 'dot-terminated'; everything else → 'dot-active' including null/empty defensive default. 27 tests. |
| `tests/spend-helpers.spec.js` | `purposeBucket` + `ENTITY_TYPE_LABELS` (T-fixture-coverage-tier1-2, 2026-06-01). Renders against `/design-system.html`. **purposeBucket bucket matching**: all 8 mapped buckets (TV & Radio, Digital & Online, Direct Mail, Printing, Staff & Payroll, Legal & Compliance, Events & Travel, Consulting) — every keyword in each PURPOSE_MAP entry exercised. **purposeBucket pattern-order precedence + fallback**: DIGITAL CONSULTING matches Digital & Online first (locks the PURPOSE_MAP ordering); TELEVISION CONSULTING matches TV & Radio; Other fallback; case insensitivity. **ENTITY_TYPE_LABELS structure lock**: every documented entity type (PAC/PTY/COM/CCM/ORG/CAN/IND) → expected label; unmapped returns undefined (locks consumer-side `|| 'Committee'` fallback pattern). 14 tests. |
| `tests/sankey.spec.js` | `buildSankeyModel(rec, opts)` pure adapter (Sankey Step 1, 2026-06-09). Injects `sankey.js` standalone via `addScriptTag` against `/design-system.html` and exercises the adapter in `page.evaluate` — no DOM/ECharts dependency (the `renderSankey` ECharts draw is verified visually in Step 2, not here). Fixtures are real `/totals/` records (verified live — see `strategy/sankey-data-model.md` verify-set). **Conservation** — receipt + disbursement node-sums reconcile to the reported totals to the penny on a candidate self-funder (Form 3), a party committee (Form 3X, fed-only), and a Form-3 PCC rendered via `entity:committee`. **No double-count** — the Contributions group sums the three contribution leaves only, excluding the candidate gift (already in self-funding). **Form-agnostic coalesce** — a Form-3 PCC under `entity:committee` still resolves Form-3 field names + self-funding-by-presence (guards the under-sum bug); `Transfers in` coalesces `transfers_from_affiliated_committee` (Form-3P JFC transfers). **Gates** — Gate 1 dual-account (`receipts !== fed_receipts` → `{gated, reason:'non-federal'}`), Gate 2 presidential, and the committee-only guard (a candidate with no `fed_receipts` field is NOT gated). **`sankeyHeight`** — content-adaptive clamp (320 floor, 560 cap, 120 for the gated message box). 8 tests. |

### How mocking works

**FEC API:** `tests/helpers/api-mock.js` intercepts all `/api/fec/*` requests (the Cloudflare proxy path used by `utils.js`) and returns shape-correct fixture data. Pages render fully without hitting the real API. The `SCHEDULE_A_COMMITTEES` fixture includes a 3rd row with `memo_code: 'X'` (mocked as ActBlue) so both aggregation paths are exercised — the committee contributors dedup which excludes memos, and the conduit sources dedup which only includes memos.

**Amplitude:** `tests/helpers/amp-mock.js` blocks the Amplitude CDN (SDK never loads, but the snippet's queue still works), and provides a stub `window.sessionReplay`. All `amplitude.track()` calls queue up in `window.amplitude._q`, which tests read via `page.evaluate()`.

---

## Track 2 — Smoke tests (live API)

**Command:** `npm run test:smoke` or `npx playwright test --config playwright.smoke.config.js`
**When to run:** Manually — before a deploy, after major changes, when debugging data issues.
**Speed:** 1–3 minutes (depends on FEC API response times). Will fail if FEC API is down.

### What they test

| Test | What's verified |
|------|----------------|
| MGP candidate page | Financials are non-zero, name matches, chart canvas renders, no 422 errors |
| Gillibrand (Senate) | Page loads with Senate-sized cycle switcher |
| Search for "Gillibrand" | At least one result, name in result text |
| Committee C00806174 (Marie for Congress) | Committee name appears, financial figures non-zero |
| WA-03 2024 race | Candidate cards render, MGP name appears, cycle-anchored links present |

### FEC API rate limits

The FEC API has rate limits. Smoke tests include a 45-second timeout per test to handle slow responses. Running smoke tests more than a few times in quick succession may hit rate limits.

---

## Pipeline — no automated tests

Neither the Cloudflare Worker (`pipeline/`) nor the GitHub Actions script (`scripts/ingest-indiv.js`) has Playwright tests. Both run outside the browser and produce no DOM to assert against.

**Cloudflare Worker (pas2) — manual verification:**
- R2 dashboard (Cloudflare → R2 → fecledger-bulk) — confirm `fec/pas2/{year}/pas2.csv` objects exist with non-zero size after a cron run or manual trigger
- `wrangler tail` — stream live logs during a run; look for `[pipeline] part N` progress lines and `[pipeline] complete: N parts`
- Manual trigger: `curl "https://fecledger-pipeline.sloanestradley.workers.dev/admin/pipeline/run?file=pas224"` → 202 response, then tail logs

**GitHub Actions (indiv) — manual verification:**
- GitHub → Actions → FEC indiv bulk data pipeline → Run workflow (manual trigger)
- Watch run log for `[2022] Part 1 complete` (confirms ZIP header parsing, decompression, column filter, and R2 auth all working)
- R2 dashboard — confirm `fec/indiv/{year}/indiv.csv` exists with non-zero size for all three years
- Spot-check: first line of each file should be the 14-column header (`CMTE_ID|ENTITY_TP|NAME|...`); a data row should have 13 pipes
- Confirm `fec/last_updated.json` exists in R2 after all three files complete

---

## Known expected failures

Tests are written around known-incomplete features. These are not failures — they're tracked in `test-cases.md`:

| Feature | Status |
|---------|--------|
| Spent tab on candidate.html | Not yet built — spent-loading state will show |
| Spent tab on committee.html | Stub placeholder only — "coming soon" text |
| IntersectionObserver enrichment on races.html | Observer fires in Playwright (Chromium has full IO support), but enrichment calls are intercepted by the API mock — cache population path is not exercised in automated tests. Manual verification required (DevTools → Network + localStorage). |

---

## File structure

```
playwright.config.js        — Playwright config (webServer, grep exclusion, timeouts)
package.json                — npm scripts: test, test:smoke, test:report
tests/
  helpers/
    amp-mock.js             — Amplitude mock helpers (CDN blocking + queue reader)
    api-mock.js             — FEC API mock (route intercept + fixture data)
  shared.spec.js            — Shared structural checks for all 10 pages
  candidate.spec.js         — candidate.html tests
  committee.spec.js         — committee.html tests
  search.spec.js            — search.html tests
  pages.spec.js             — all other pages (races, race, candidates, committees, feed, process-log, design-system, index, mobile)
  overlay.spec.js           — global search overlay (T-search-overlay)
  tooltip.spec.js           — Tooltip component (T-tooltip-component)
  party-helpers.spec.js     — partyClass + partyLabel dual-field design (T-party-helpers-dual-field-rewrite)
  candidate-card.spec.js    — candidateCardHTML consolidated builder (T-card-builder-consolidation)
  race-helpers.spec.js      — formatRaceName + formatRaceLabelLong + toOrdinal (T-fixture-coverage-tier1-2)
  committee-helpers.spec.js — committeeTypeLabel + filingFrequencyLabel + filingFrequencyDotClass (T-fixture-coverage-tier1-2)
  spend-helpers.spec.js     — purposeBucket + ENTITY_TYPE_LABELS (T-fixture-coverage-tier1-2)
  smoke.spec.js             — @smoke live API tests
```

---

## Adding new tests

### For a new page
1. Add the page to the `PAGES` manifest in `shared.spec.js` with its expected active nav item.
2. Add a new `test.describe` block in `pages.spec.js` for page-specific checks.
3. Add a smoke test in `smoke.spec.js` tagged `@smoke`.

### For a new API endpoint
Add a fixture to `tests/helpers/api-mock.js` in `resolveFixture()`. Follow the existing URL pattern matching order (specific before generic).

### For new Amplitude events
Use `findTrackEvent(page, 'Event Name')` from `amp-mock.js`. Events are captured in `window.amplitude._q` since the Amplitude SDK is blocked — they queue up there for the duration of the page session.

### Testing hash navigation while preserving scrollY
Use `page.evaluate(() => { window.location.hash = '#year#tab'; })` instead of `element.click()` when a test needs to trigger hash navigation AND check `window.scrollY` afterward. Playwright's `element.click()` auto-scrolls the target element into view before the click fires, resetting `scrollY` to a browser-controlled value before the page's JS can read it. The `page.evaluate` approach changes the hash directly without triggering scroll-into-view. Used by the compact-header transition tests in `candidate.spec.js`.
