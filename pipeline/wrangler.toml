name = "fecledger-pipeline"
main = "src/index.js"
compatibility_date = "2024-09-23"

# Fill in before deploying:
#   npx wrangler whoami
account_id = "ebc968a93e3bec33c52c5121c914cc77"

[[r2_buckets]]
binding = "BULK"
bucket_name = "fecledger-bulk"

# Cron trigger removed 2026-04-16 — all file processing moved to GitHub Actions
# (scripts/ingest-bulk.js, .github/workflows/fec-bulk-pipeline.yml).
# The fetch handler (/admin/pipeline/run) remains for ad-hoc testing.
